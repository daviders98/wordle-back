services:
  - type: web
    name: wordle-backend
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wordle_back.wsgi:application --bind 0.0.0.0:$PORT
    autoDeploy: true